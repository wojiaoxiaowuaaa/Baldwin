pgrep -fl main.py # 字符创形式匹配查询进程信息

ps -p 49504   # 精准按PID查看进程详情

pkill -f main.py # 终止命令行里包含 `main_agent.py` 的运行进程

sudo netstat -tuln | grep 5000 # 监听指定端口的流量信息，可以用 ss -tuln | grep :5000替代

top -o mem  # 内存占用排序

find . -type f -name "*.log" -delete # 删除指定目录下所有指定后缀的文件

cp  -r  source_directory  destination_directory # 递归复制目录

du  -sh  directory  # 显示目录的总大小

tail  -f  demo.log  # 实时跟踪展示日志的最新内容less  demo.txt分页查看

grep -ri "example" # 大小写不敏感,在指定目录中寻找字符串  ack(与grep类似可视化更好)

grep demo test.log  # 在文件中查找指定字符串

find  .  -name  "*.py"  # 递归查找当前目录及其子目录下的指定后缀的文件
 
find .  -size  +100M  # 递归查找当前目录及其子目录下大于10M的文件 sudo find / -xdev -type f -size +1G -print 2>/dev/null  # 从根目录开始找大于 1G 的文件
 
tar  -zcvf  demo.tar.gz  Desktop  # 打包指定的目录
 
ps -ef | grep 3306  # 查看指定端口上的进程信息

lsof -i:8089  # 查看端口上的进程信息 然后输入kill -9 pid  杀进程

hostname -I  # 只输出当前主机的 IPv4 地址（简洁版）(完整版debina系统 ip a）

curl ifconfig.me / curl ipinfo.io/ip  # 查看公网 IP（需联网）  

hostname -I  # 查看内网ip   

sudo launchctl list | grep com.company.test.service  # MAC查看服务

sudo tcpdump -i any -nn 'host 1.1.1.1' # 抓与指定IP有关的包

sudo dscacheutil -flushcache  # sudo killall -HUP mDNSResponder 清空缓存重启DNS

----------------------------------------------------------------------------------------------



dd if=/dev/zero bs=1048576 count=1000 | gzip > boom.gz  (解压得到大文件1G:  gzip -d boom.gz)  # 生成测试压缩文件文件  dd if=/dev/zero of=output_file bs=1M count=24  # 生大小24M  文件名output_file

find .  -name   "*.zip"  -exec unzip -o {} \;  # 解压当前目录下的所有zip文件,末尾的分号记得带上

pip3 install httpstat (brew install httpstat)  # 安装HTTP工具(优化请求结果展示)：

python3 -m http.server  # 在指定目录下启动简易服务器 可通过浏览器访问  python3 -m http.server --bind 0.0.0.0 8080

cd ~ && mkdir .pip && touch pip.conf  && vi pip.conf [global] index-url=https://指定的pip源 extra-index-url = https://添加多个源

